// MCP tools removed — all capabilities migrated to CLI subcommands + Skills.
// send_to_thread → botmux send
// get_thread_messages → botmux thread messages
// list_bots → botmux bots list
export const tools = {} as const;
